package cn.mesa.utils

import java.io.FileInputStream
import java.sql.Date
import java.text.SimpleDateFormat
import java.util
import java.util.Properties

object ConfigUtils {
//  private val props: Properties = new Properties
//  val path = Thread.currentThread().getContextClassLoader.getResource("application.properties").getPath
//  props.load(new FileInputStream(path))
//  def getString(propName:String) = props.getProperty(propName)
//  def getInt(propName:String):Int = props.getProperty(propName).trim.toInt
//  def getDate(propName:String): Date = {
//    val timeString = props.getProperty(propName)
//    val timeUtil = new SimpleDateFormat("yyyy-MM-dd").parse(timeString)
//    val resultDate = new Date(timeUtil.getTime)
//    resultDate
//  }

}
